import "std/core.s"
import "tests/foo.s"

def pair(nat a, nat b)
    return compiler::args()

def main()
    CLI = edit console()
    p = pair(0,1)
    x = mut 0
    x = x+1
    print x

    print p.a
    
    print foo

     