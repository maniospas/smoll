import std.core
import std.ptrpeek

def point(nat x, nat y)
    return class compiler::args()

def main()  
    CLI = edit console()
    a = last [point(0,0)]
    b = last [point(1,1)]
    debug::print a
    debug::print b
    print a.x+b.x
